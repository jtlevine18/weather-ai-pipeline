# Core
python-dotenv==1.0.0
httpx==0.27.0
rich==13.0.0
imdlib==0.1.21

# ML / Forecasting
xgboost==2.0.0
scikit-learn==1.5.0
numpy==2.0.0
scipy==1.14.0

# Weather models (Google DeepMind — requires GPU)
# GraphCast 0.25° (primary, needs A100 80GB); NeuralGCM 2.8° (fallback, fits L4 48GB)
graphcast @ git+https://github.com/google-deepmind/graphcast.git
dm-haiku>=0.0.12
neuralgcm==1.2.2
jax[cuda12]==0.4.27
google-cloud-storage
gcsfs>=2024.2.0
xarray>=2024.10.0
zarr>=2.16.0

# RAG / NLP
faiss-cpu==1.9.0
sentence-transformers==2.7.0
langchain-huggingface==0.0.3

# Claude API
anthropic==0.34.0

# Data processing
pandas==2.2.2

# Data contracts
pydantic==2.6.0

# Scheduling
apscheduler==3.10.0

# Orchestration (Dagster)
dagster==1.7.0
dagster-webserver==1.7.0

# PostgreSQL
psycopg2-binary==2.9.9

# API + Auth
fastapi==0.111.0
uvicorn==0.29.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.0

# Optional: Twilio SMS
# twilio>=9.0.0

# Production
slowapi==0.1.9
prometheus-fastapi-instrumentator==7.0.0
python-json-logger==2.0.7
tenacity==8.2.3
